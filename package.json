{
  "name": "apatejs-retro",
  "version": "1.0.0",
  "description": "A simple 2D retro game engine which runs directly in your browser",
  "main": "dist/apate.js",
  "typings": "dist/apate.d.ts",
  "scripts": {
    "build": "rimraf dist/ && rollup --config rollup.config.js",
    "build/dev": "rollup --config rollup.config.js --environment BUILD:development",
    "clean": "rimraf dist/",
    "dev": "nodemon --config nodemon.json",
    "test": "eslint src/**/*.ts && prettier src/**/*.ts --check",
    "lint": "eslint src/**/*.ts && prettier src/**/*.ts --check",
    "format": "prettier src/**/*.ts --write"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juiian7/ApateJS-Retro.git"
  },
  "author": "juiian7",
  "license": "GPL-3.0",
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.5.0",
    "@typescript-eslint/eslint-plugin": "^5.62.0",
    "@typescript-eslint/parser": "^5.62.0",
    "eslint": "^8.57.1",
    "nodemon": "^3.1.10",
    "prettier": "^3.5.3",
    "rimraf": "^6.0.1",
    "rollup": "^2.79.2",
    "rollup-plugin-dts": "^4.2.3",
    "typescript": "~4.5.5"
  },
  "prettier": {
    "singleQuote": false,
    "tabWidth": 4,
    "printWidth": 180,
    "trailingComma": "es5",
    "endOfLine": "auto"
  }
}
